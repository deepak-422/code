#include <bits/stdc++.h>
using namespace std;
#define ll long long
ll maxll(ll a,ll b){
    if(a>=b)return a;
    return b;
}
ll binexpo(ll a,ll b){
    ll ans=1;
    while(b){
        if(b&1){
            ans=ans*a;
        }
        b=b>>1;
        a=a*a;
    }
    return ans;
}

ll join(ll n1,ll n2){
    while(n2){
        n1=n1*10+(n2%10);
        n2/=10;
    }
    return n1;
}

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll n;cin>>n;
    vector<ll> v;
    bool stop=false;
    for(int digit=1;digit<=5 && !stop;digit++){
        ll start=binexpo(10,digit-1);
        ll end=binexpo(10,digit)-1;
        for(int j=start;j<=end;j++){
            ll pal1=join(j,j);
            ll pal2=join(j/10,j);
            if(pal1 >n && pal2>n){
                stop=true;break;
            }
            if(pal2<=n)v.push_back(pal2);
            if(pal1<=n)v.push_back(pal1);
            
        }

    }
    sort(v.begin(),v.end());
    for(auto ele:v){
        cout<<ele<<endl;
    }
    

}
